import { AddPropertyPipe } from './add-property.pipe';

describe('AddPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new AddPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
